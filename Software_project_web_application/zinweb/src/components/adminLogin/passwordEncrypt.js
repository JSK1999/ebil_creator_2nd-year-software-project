/*import React from 'react';
import {useRef} from "react";
import bcrypt from 'bcryptjs';

const PasswordEncrypt=() => {
const emailInputRef= useRef();
const passwordInputRef=useRef();

  return (
<form>
    <input type ='email' placeholder='email' ref={emailInputRef} style={{ padding : '15px',borderRadius:'10px ',margin: '10px'}}></input>
   
    <input type ='password' placeholder='password' ref={passwordInputRef} style={{ padding : '15px',borderRadius:'10px ',margin: '10px'}}></input>
    <button>
        signup
    </button>
</form>
     );
};

export default PasswordEncrypt*/